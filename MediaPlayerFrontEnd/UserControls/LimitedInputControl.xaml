<UserControl x:Class="MediaPlayerPL.LimitedInputControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MediaPlayerPL"
             mc:Ignorable="d" 
             d:DesignHeight="250" d:DesignWidth="500">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <!-- Getting styles from the resource dictionary-->
                <ResourceDictionary Source="../Styles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    <Grid>
        <Label Content="{Binding Title}" />
        <Label>
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="{Binding ElementName=TitleLimitedInput, Path=Text.Length}" />
                <TextBlock Text="/" />
                <TextBlock Text="{Binding MaxLength}" />
            </StackPanel>
        </Label>
        <TextBox Style="{StaticResource TextBoxStyle}" MaxLength="{Binding MaxLength}" Name="TitleLimitedInput" ScrollViewer.VerticalScrollBarVisibility="Disabled" TextWrapping="NoWrap" />
    </Grid>
</UserControl>
